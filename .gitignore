# Environment & Secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies
node_modules/
package-lock.json
yarn.lock

# Logs
*.log
logs/

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.sublime-project
.sublime-workspace

# Build output
# COMMITTED build artifacts (the "fork & go" ethos — shipped, not ignored):
#   legacy/dist/        — pre-transpiled JSX the legacy app loads (scripts/build-jsx.mjs)
#   vendor/codex-llm.cjs — @codex/core/llm bundled to CJS for the Node server (scripts/build-server-llm.mjs)
build/
*.bundle.js

# Testing
coverage/
.nyc_output/

# Temporary
*.tmp
.cache/
.claude/

# BTC donation pool — server-side state, never commit
data/donations.json
data/donations.json.tmp
data/donations.audit.log

# Internal dev artifacts (architecture map + foundation plan — not for the public PWA)
_arch-viz.html
FOUNDATION.md
dist-web/

